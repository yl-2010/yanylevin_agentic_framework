import SwiftUI

enum WidgetTheme {
    /// Always-light chrome so text stays readable regardless of system dark mode.
    static let bg0 = Color(red: 0xE8 / 255, green: 0xED / 255, blue: 0xF2 / 255)
    static let bg1 = Color(red: 0xDD / 255, green: 0xE5 / 255, blue: 0xEC / 255)
    static let fg = Color(red: 0x14 / 255, green: 0x18 / 255, blue: 0x1D / 255)
    static let muted = Color(red: 0x5A / 255, green: 0x64 / 255, blue: 0x70 / 255)
    static let accent = Color(red: 0x1B / 255, green: 0x7D / 255, blue: 0x8A / 255)
}
